# Observability

OpenTelemetry (OTel) is treated as **evidence plumbing**, not just “monitoring”.

Targets:
- consistent `correlation_id` and OTel trace propagation across async boundaries
- collector config in `observability/otel-collector.yaml`
- dashboards later in `observability/dashboards/`

