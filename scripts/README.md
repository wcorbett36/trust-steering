# Scripts

Scripts are intentionally small and local-first.

- `scripts/up.sh` / `scripts/down.sh`: bring local stack up/down (placeholder at bootstrap)
- `scripts/test.sh`: best-effort local checks (OPA tests when installed)
- `scripts/export_audit_packet.sh`: export evidence for review (placeholder at bootstrap)

