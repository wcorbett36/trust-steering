# k3d (planned)

Placeholder for local Kubernetes cluster instructions and manifests.

Target later:
- `scripts/up.sh` creates cluster
- local registry (optional)
- deploy stream (Redpanda), OPA, OTel collector, and the services

