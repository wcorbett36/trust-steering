# Infra

Local-first environment scaffolding (choose one):
- `infra/k3d/` for k3d-based Kubernetes
- `infra/kind/` for kind-based Kubernetes

GitOps (Argo CD/Flux) and Helm charts can be added later under `infra/`.

