# Specs (optional)

Use this directory for small, focused specs that capture invariants:
- “no action without authorization”
- “every action is traceable”
- “idempotent execution for correlation_id”

TLA+ is a good fit for one or two critical workflows once the system shape is concrete.

