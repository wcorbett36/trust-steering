# Supply chain evidence (planned)

This area will hold:
- SBOM generation instructions and outputs (`supplychain/sbom/`)
- signing + verification steps (`supplychain/signing/`)
- (later) provenance attestations tied to commits/build recipes

